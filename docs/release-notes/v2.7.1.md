# 剪忆 ClipMemory v2.7.1 — Search Performance & Cryptographic Safety / 搜索性能与密码安全加固

> ⚠️ **YANKED 2026-07-29** — 这个版本已于 2026-07-29 撤回。原因：标题宣称的 *Search Performance* 工作（fuzzy search + image integrity scan）实际并未进入 tag。撤回后仅 *Cryptographic Safety*（P0-1/2/3）在 v2.7.1 binary 中。
>
> **请改装 v2.7.2**：v2.7.2 含有 fuzzy search + image integrity scan + 完整 Cryptographic Safety。
>
> **Why yanked**: The release title claimed Search Performance & Cryptographic Safety, but v2.7.1 was tagged before the search performance work was merged. The actual v2.7.1 binary contains only the Cryptographic Safety changes (P0-1/2/3 decryptWithReason, diagnostics banner, Keychain unlock retry).
>
> **Install v2.7.2 instead**: v2.7.2 includes fuzzy search + image integrity scan + the full Cryptographic Safety changes.

**发布日期 / Release Date**: 2026-07-29
**撤回日期 / Yanked Date**: 2026-07-29
**类型 / Type**: Yanked release (do not install)
**替代版 / Replacement**: v2.7.2

## 中文

### 主要更新 (Highlights)

- **搜索速度大幅提升** — 主线程不再同步解密；缓存预热覆盖 App 唤醒、新条目捕获、列表首次显示，冷启动搜索不再卡顿
- **加密失败诊断** — 当 Keychain 锁定导致解密失败时，搜索页显示黄色诊断横幅说明原因，不再静默空结果
- **解密失败不会永久标记条目** — 修了一个潜伏 bug：Keychain 瞬态锁定后条目曾被永久标记为「不可解密」，现在 key 恢复后可自动重试

### 修复 (Fixes)

- **DI 容器安全性** — `ServiceContainer.crypto` 改为 `private(set)`，防止非测试代码误替换加密服务
- **AES-CBC 实现合并** — 删除 ImageStorage 中重复的 CBC 解密实现，统一走 CryptoService 的单一路径
- **Swift 严格并发** — 启用 `-strict-concurrency=minimal`，编译器级保障 actor 隔离
- **图片去重优化** — 同图多次复制不再产生重复文件 + 列表条目

### 升级提示 (Upgrade Note)

- v2.4.0 起带自动升级模块（Sparkle）的版本：等 App 内自动更新，或 `brew upgrade --cask clipmemory`

## English

### Highlights

- **Significantly faster search** — decrypts no longer block the main thread; cache prewarming covers app wake, new item capture, and list first-display
- **Decryption failure diagnostics** — a yellow banner now explains why search results are empty when the Keychain is locked, replacing silent empty results
- **Transient Keychain lock no longer permanently marks items** — fixed a latent bug where items were permanently labeled "undecryptable" after a transient Keychain lock

### Fixes

- **DI container hardening** — `ServiceContainer.crypto` now `private(set)`, preventing non-test code from swapping crypto services
- **AES-CBC implementation deduplication** — removed duplicate CBC decrypt in ImageStorage, all legacy decryption goes through CryptoService
- **Swift strict concurrency** — enabled `-strict-concurrency=minimal` for compile-time actor isolation guarantees
- **Image dedup optimization** — repeated copies of the same image no longer create duplicate files or list entries

### Upgrade Note

- Versions with the auto-update module (Sparkle, v2.4.0+): wait for in-app auto-update, or run `brew upgrade --cask clipmemory`

## 安装 / Install

```bash
# Homebrew Cask
brew tap irykelee/clipmemory
brew trust irykelee/clipmemory
brew install --cask clipmemory

# Manual download
# https://github.com/irykelee/clipmemory/releases/download/v2.7.1/ClipMemory.tar.gz
```

> **首次打开若提示「Apple 无法验证…」/ If macOS blocks the first launch with "Apple cannot verify…"**：这是 macOS 对未公证应用的常规拦截，不是病毒。右键点 App →「打开」→ 再点「打开」；或 系统设置 → 隐私与安全性 →「仍要打开」。仅需操作一次。/ This is the standard prompt for non-notarized apps, not malware. Right-click the app → **Open** → **Open** again; or System Settings → Privacy & Security → **Open Anyway**. Only needed once.
